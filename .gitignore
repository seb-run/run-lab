# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/

# macOS
.DS_Store
._*

# IDE
.vscode/
.idea/

# Cache local (les données utilisateur sont hors repo, dans ~/Documents/SebMetrics/)
*.cache.json

# Logs
*.log

# Backups locaux
data/config.json.backup

# Fichiers temporaires d'écriture atomique (restes de crash)
*.json.new

# Notes de travail perso — NE PAS publier (repo public)
NOTE_PRODUIT_run-lab.md
econ.py

# Fichiers .fit bruts : la donnée utile part dans sessions_cache.json
data/fit_inbox/

# Captures du contrôle visuel (scripts/verif_ui.py) : regénérées à la demande
output/captures/

# Sauvegardes du dédoublonnage (scripts/dedupe_cache.py --write)
data/sessions_cache.avant_dedup_*.json
